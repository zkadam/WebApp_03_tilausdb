@model IEnumerable<WebApp_03_tilausdb.Models.tilaus_maaria_arkipaivissa>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.viikkopaiva)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.number_of_orders)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.viikkopaiva)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.number_of_orders)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.Rivinro }) |
                @Html.ActionLink("Details", "Details", new { id=item.Rivinro }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.Rivinro })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
